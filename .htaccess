<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Route all requests starting with /api/ to api.php
    # Example: /api/db -> api.php?route=db
    RewriteRule ^api/(.*)$ api.php?route=$1 [QSA,L]
</IfModule>
